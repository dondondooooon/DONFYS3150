#include <iostream>
#include <armadillo>
using namespace arma;
using namespace std;


int main(){
    int L, n_samples, cycles, Temp;
    L = 2;
    n_samples = 10;
    cycles = 2; 
    Temp = 1; 

    mat A = mat(L,L).fill(0.);

}